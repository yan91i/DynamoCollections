Join in Views

📎 Join the elements (walls, floors, columns, ...) in the selected views by category. Set the joining order (which categories should cut others) using ↓↑.

Inputs:

Select View/s.
Select Category/s.
Joining Order.
Join by Selection.
Version: 1.2.0