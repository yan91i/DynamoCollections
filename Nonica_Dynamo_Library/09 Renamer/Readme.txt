ReNamer

ID Rename multiple views, sheets, schedules, grids, rooms and much more (in seconds). You can replace text, add a prefix or a suffix to your selection. Preselect more than one element (views, sheets, rooms, ...) in Revit in order to automatically precheck those elements in the Renamer window.

Inputs:

Select category.
Check elements from category.
Replace text.
Define prefix and suffix.
Version: 1.7.0