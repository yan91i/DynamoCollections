Duplicator

📄 Duplicate multiple sheets, schedules, groups or views (in seconds). You can specify a prefix, a suffix and which elements you want to include when duplicating sheets such as legends, schedules or texts. Preselect more than one view or sheet in Revit in order to automatically check those elements in the Duplicator window.

Inputs:

Select category.
Check elements from category.
Sheet options.
Replace text.
Define prefix and suffix for names or numbers.
This script was based on Duplicate Sheets by Erik Frits
Version: 1.8.0